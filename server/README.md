# ShreeTech CRM — Backend (Server)

This folder contains the Node.js + Express backend API.

Built with:
- Node.js 18+
- Express.js (API server on port 3000)
- PostgreSQL via Prisma ORM
- CommonJS module system (require/module.exports)

Setup instructions will be added on Day 3.